defmodule Frex.Client.ReceiptsTest do
  use ExUnit.Case, async: true
  # alias Frex.Client.Receipts
  # import Credentials

  # test "Receipts.get calls are successful" do
  #   {status, _} = Receipts.get(credentials, 00000063975)
  #   assert status == :ok
  # end
end
