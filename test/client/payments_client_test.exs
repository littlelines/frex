defmodule Frex.Client.PaymentsTest do
  # use ExUnit.Case, async: true
  # alias Frex.Client.Payments
  # import Credentials

  # TODO: Make this work!
  # test "Payments.get calls are successful" do
  #   {status, _} = Payments.get(credentials, 1)
  #   assert status == :ok
  # end

  # test "Payments.list calls are successful" do
  #   {status, _} = Payments.list(credentials)
  #   assert status == :ok
  # end
end
